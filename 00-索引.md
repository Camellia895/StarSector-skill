# Starsector Mod 工作流索引（唯一入口）

> 技能库根 `<skills>` = `C:\game\StarSector.v0.9.8a-RC8\_work\skills`
> 游戏根 `<game>` = `C:\game\StarSector.v0.9.8a-RC8`（实际版本 **0.98a-RC8**）

## 怎么用（省上下文的加载顺序）

1. 先读**本文件**（≤80 行）定位任务分类。
2. 只读**一个** `workflows\wf-*.md`（它写清步骤顺序、闸门、产出）。
3. 按该 wf 点名的 2–4 个文件去 `read`：`skills\<name>\SKILL.md` 或 `shared\*.md`。
4. **不要**通读 `skills\` 整个目录；**不要**让别的文件重复 `shared\env.md` 的内容。
5. 脚本一律在 `<skills>\shared\scripts\`（登记表：`shared\script-registry.md`）。

## 三类任务 → 读哪个

| 你要做的事 | 读 | 一句话 |
|---|---|---|
| **① 汉化一个 mod**（提取文本 → 交用户汉化 → 合并汉化） | `workflows\wf-localize.md` | 提取出待译清单交给用户；用户译完由 AI 注入并逐层验证 |
| **② 汉化迁移**（mod 更新版本，有旧汉化可复用） | `workflows\wf-translate-update.md` | 先复用旧译；按**新内容占比**分三档：<10% 仅告知 / 10–20% 汉化完交检阅 / ≥20% 不汉化只整理 |
| **③ 给 mod 更新版本以适配新版游戏** | `workflows\wf-game-update.md` | 环境取证 → 编译探测 → jar↔源码证明 → 数据/引用/语义核查 → 修复重建 → 交付；**mod 目标版本 ≤0.8x 必读 game-upgrade skill §7 专项 + 读档冒烟** |
| **④ 出事了**（崩溃 / 漏译 / 显示 `?` / 文本截断） | `workflows\wf-diagnose.md`；**启动就崩 / Fatal** → 先读 `workflows\wf-launch-audit.md` | 症状 → 对应诊断 skill 与校验脚本；启动期崩溃多为 R12/R13/R15，有专门查表 |
| **⑤ 校验该不该继续跑**（触发词"校验审计"） | `workflows\wf-audit-checks.md` | 账本体检 + 探针复查 → 升档/降档/休眠（省上下文的关键） |
| **⑥ 优化技能库本身**（新增/改写 skill、裁剪校验） | `skills\skill-authoring\SKILL.md`（§7 演化 + §8 体检） | 把经验固化成规则、修错路由、拆分过长的 skill |
| （③ 之后要重新汉化） | `workflows\wf-game-update.md` §衔接 → `wf-translate-update.md` | 改字符串/重编译会丢汉化，须重打补丁 |

## 支撑文件

| 文件 | 内容 |
|---|---|
| `shared\env.md` | （路径 / JRE / JDK / 工具链 / 编码 / 五条环境毒点：`-noverify`、logs 路径、class 版本、**PowerShell 变量名大小写不敏感**、`.ps1` 编码） |
| `shared\iron-rules.md` | （R1 CSV 引号 / R2 rules 参数引号 / R3 字库 / R4 `%%` / R5 `${}` / R6 `\u0001` / R7 标识符 / R8 伪 JSON / R9 zip 斜杠 / R10 术语一致 / R11 加权词表 / **R12 逻辑键绝不译** / **R13 options 结构等价** / **R14 提取完整性（recipe 有盲区）** / **R15 注入前断言英文原版** / **R16 设计类型名三通道** / **R17 保持原文件行尾风格** / **R18 格内子串替换的 needle 边界**） |
| `shared\conventions.md` | 工作目录约定 / 命名 / **条目计数口径** / worklist 形态 / **闸门 G1–G6** / 增量复用 / 上下文纪律 |
| `shared\verification-ledger.md` | **校验记账本与档位**（哪些校验每次跑、哪些抽样、哪些休眠；severity × 命中 两轴计票，探针防"温室效应"） |
| `shared\glossary.md` + `glossary.json` | 术语表（人读带来源 / 脚本读） |
| `shared\script-registry.md` | 全部脚本的用途与用法 |
| `reference\SSTLib_API文档\` | SSTLib API 文档（HTML，模组开发用） |
| `_archive\` | 废弃 skill 与重复副本（**只归档不删除**） |

## 执行层 skill 一览

| skill | 干什么 | 何时用 |
|---|---|---|
| `starsector-mod-localization-extract` | 提取待译清单（data 层 + jar 层） | 任务①②的第一步 |
| `starsector-mod-localization-migrate` | 从旧汉化抽 EN→ZH 映射 | 任务②（有旧汉化时） |
| `starsector-mod-localization-apply` | 译文注入 + 常量池补丁 + 重打包 | 拿到译文之后 |
| `starsector-mod-localization-verify` | 闸门 G2–G6 逐层验证 | 每次改动后、交付前 |
| `starsector-mod-localization-content` | 译文质量规范（界面/功能文本） | 翻译与审校时 |
| `starsector-mod-localization-spec` | 写作期格式铁律（占位符/引号/字符集） | 动笔前必读 |
| `starsector-translation-voice` | 对话与叙事文风、角色声线 | 对话/背景/人物文本 |
| `starsector-mod-game-upgrade` | 旧版 mod 升级到 0.98a-RC8 | 任务③ |
| `starsector-mod-render-fix` | 渲染坐标迁移专项 | 任务③ 命中该症状时 |
| `starsector-mod-kotlin-rebuild` | Kotlin mod 重编译链 | 需要从源码重建 jar 时 |
| `starsector-mod-java-hardcoded-text` | **Java 硬编码文本**：重编译 vs 常量池补丁决策 + 流式条目 jar 解析 + 等价性证明 | 文案写死在 `.java`/jar 里时（纯 Java mod） |
| `starsector-mod-janino-source-localization` | **Janino 源码层汉化**（无 jars、`data/plugins|scripts/*.java` 运行时编译）：字面量提取双层网 + `%` 逐行判据 + 注入三坑 + 骨架等价闸门 + JSON 引号事故 | 摸底发现 mod 无 `jars/` 且 Java 源码直接在 data 下时 |
| `starsector-engine-diagnose` | 引擎行为复现 / 语义定论 | 崩溃排查、拿不准是否该改 |
| `starsector-jar-decompile` | CFR 反编译 | 需要读 jar/class 实现时 |
| `starsector-repo-source` | 取上游源码基线（fork/tarball） | 需要源码时 |
| `starsector-mod-star-system` | CSS 自定义星系制作 | 新星系 mod |
| `starsector-mod-delivery` | 交付四项 + 打包 zip | 每个任务的最后一步 |
| `skill-authoring` | **本技能库怎么写/怎么演化**：分层归属、SKILL.md 模板、description 可路由性、新建门槛、变更纪律 | 要新增或改写 skill 时；说"优化 skill 库"时 |

## 四条通用纪律

1. **先取证再动手**：术语查 `glossary.md` + 核心数据的实际用词；数据/API 行为先反汇编或读 `starfarer.api.zip`，不联网猜。
2. **不重复验证已知事实**：`env.md` 里写过的（路径、JDK 版本、`-noverify`、日志 GBK…）直接用；只有报错才回头复核。
3. **每个任务结束都留档**：EN→ZH 映射、术语表补充、脚本、`excluded_entries.json` 落 `_work\mod_work\<Mod>\`，
   下次同 mod 更新才不用重做（`conventions.md` §6）。
4. **崩完要复盘到 skill 库**：凡"闸门没拦住"的错误，必须补一道闸门并登记（`script-registry.md` D 节 +
   `verification-ledger.md`），**不要**只把结论留在 `_work\mod_work\<Mod>\` —— 会随项目一起被遗忘。
   流程见 `workflows\wf-launch-audit.md` §6。
   


