# {mod 中文名} 项目说明

> 本文件说明本 mod 的结构，供协作者/AI 快速上手。游戏版本 {0.98a-RC8}，依赖 {依赖列表}。

## 功能

{一句话功能说明。例：将 X 内容中文化/新增 Y 玩法/修复 Z。}

## 基本信息（mod_info.json）

- id：`{mod id}`
- 版本：{版本号}
- 依赖：{LunaLib / LazyLib / MagicLib / 无}
- modPlugin：{插件类；若纯数据 mod 则写"无"}

## 目录结构

```
mods/{mod 文件夹}/
├── mod_info.json                  # mod 元信息（id/名称/版本/依赖/插件入口）
├── 项目说明.md                    # 本文件
└── data/                          # 数据（CSV/JSON/脚本）
    ├── ...                        # 按实际结构逐项说明作用
    └── ...
（若有 graphics/、jars/、sounds/、localization/ 等，逐项列出）
```

## 配置说明（用户可编辑）

- {列出用户可编辑的数据/配置文件，说明每项的用途与约束。}

## 常见操作

| 操作 | 方法 |
|---|---|
| 启用/验证 | {启动器勾选 → 游戏内验证步骤} |
| 更新内容 | {改哪些文件、如何校验} |
| 重新打包 | `powershell -File <delivery-skill>\scripts\deliver.ps1 -ModPath mods\{mod 文件夹}` |
| 版本记录 | 本目录 git：`git log` / `git status`（每次改动后提交） |

## 备注

- 本 mod 目录是本地 git 仓库（`.git` 不随交付 zip 打包）。
- {其他需要说明的事项，如依赖 mod 的版本要求、已知限制。}
